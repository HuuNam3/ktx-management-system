import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Frontend_Management_System_KTX',
  webDir: 'www'
};

export default config;
